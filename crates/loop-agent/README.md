# loop-agent

Stateful agent with tool execution and event streaming.

This crate is a **stub** in phase 1. It will own the agent loop, tool runners,
and higher-level agent events once `loop-ai` is stable.
