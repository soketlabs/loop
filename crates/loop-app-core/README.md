# loop-app-core

Shared application layer for Loop: configuration paths, auth, settings, bootstrap into `AgentHarness`, tool approval, themes, and resource loading.

Used by `loop-cli` and `loop-desktop`.
