# loop

Production-grade AI harness in Rust.

| Crate | Role |
|-------|------|
| `loop-ai` | Unified LLM API |
| `loop-agent` | Stateful agent (stub) |
| `loop-cli` | Interactive coding agent CLI (stub) |

## Build

```bash
cargo build
cargo test -p loop-ai
```
